# Compilation libraries configuration
Import craftbukkit.jar into the folder ./jars (create it if necessary)

# Compilation
Use headblocks-compile.bash to compile on Linux
Use headblocks-compile.bat to compile on Windows
